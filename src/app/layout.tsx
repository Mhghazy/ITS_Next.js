import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ITS Group - Integrated Technical Services | Swimming Pools Egypt",
  description: "Expert swimming pool construction, maintenance & design in Egypt. 30+ years of experience in residential & commercial pools. Custom designs, professional installation, reliable after-sales support.",
};

import SiteHeader from "@/components/SiteHeader";
import Footer from "@/components/Footer";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <SiteHeader />
        {children}
        <Footer />
      </body>
    </html>
  );
}
// Force layout rebuild
